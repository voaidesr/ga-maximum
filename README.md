# GA maximum

## Setup

Ensure Haskell and Cabal are installed. Easiest way is through GHCup.

```bash
curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | sh
```

To run

```bash
cabal run
```
